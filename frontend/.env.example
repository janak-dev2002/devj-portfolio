# Base URL of the backend API.
#
# Local dev (backend running standalone on localhost:8080):
VITE_API_URL=http://localhost:8080
#
# Production build: leave unset (or empty). Nginx serves the SPA and proxies
# /api/* to the backend on the same origin, so requests can stay relative.
