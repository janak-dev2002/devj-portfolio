# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key

# Ansible — real inventory/vault, never committed
devops/ansible/inventory.ini
devops/ansible/group_vars/**/vault.yml

# MCP config — contains credentials, never commit
.mcp.json
.gemini/settings.json

# Internal ZoneForty5 coordination files — not project deliverables
shared/
.claude/
.gemini/

# OS
Thumbs.db
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Frontend build (has its own .gitignore)
frontend/node_modules/
frontend/dist/

# Backend build (Maven)
backend/target/
*.class
