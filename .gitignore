# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key

# MCP config — contains credentials, never commit
.mcp.json
.gemini/settings.json

# Internal ZoneForty5 coordination files — not project deliverables
shared/
.claude/
.gemini/

# OS
Thumbs.db
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Frontend build (has its own .gitignore)
frontend/node_modules/
frontend/dist/

# Backend build (Maven)
backend/target/
*.class
